/**
 * @labourchain/repository
 *
 * Repository capabilities are composed as Cordis plugins. This package entry
 * currently exposes the executable node bootstrap primitives; domain plugins
 * are added by later capability stories.
 */
export * from './bootstrap.ts'
