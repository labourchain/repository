/**
 * @labourchain/repository
 *
 * Spec-driven Cordis plugin scaffold.
 * Repository behavior is defined by specs before implementation.
 */
import type { Context } from '@deepseek-ai/cordis'

export const name = 'labourchain-repository'

/**
 * Bootstrap-only plugin entry point.
 *
 * SPEC-0001 defines the first usable Repository service. Until that service is
 * implemented and reviewed, this package remains private and activation owns
 * no external resources or process-global state.
 */
export function apply(_ctx: Context): void {}
